image: registry.gitlab.com/elypia/elypia-emotes/imagemagick

cache:
  key: $CI_PIPELINE_ID

stages:
  - test
  - build
  - deploy

test:
  image: alpine:3.9.4
  stage: test
  script: 
    - sh scripts/test.sh

build:
  stage: build
  script:
    - bash scripts/build.sh
  cache:
    policy: push
    paths:
      - output/
  artifacts:
    paths:
      - output/

pages:
  stage: deploy
  script:
    - sh scripts/pages.sh
  cache:
    policy: pull
    paths:
      - output/
  artifacts:
    paths:
      - public/
